# Windows-Compatible Requirements - Fixed Version
# Use this file if Pillow installation fails

# Core Flask packages (always work)
Flask==2.3.3
Flask-SQLAlchemy==3.0.5
Flask-Login==0.6.3
Flask-WTF==1.1.1
Flask-Mail==0.9.1
WTForms==3.0.1
Werkzeug==2.3.7
email-validator==2.0.0
python-dotenv==1.0.0
gunicorn==21.2.0

# Database
PyMySQL==1.1.0
bcrypt==4.0.1

# Try these without version constraints first
# If they fail, we'll install them separately
qrcode
python-socketio
Flask-SocketIO

# Note: We'll install Pillow separately with special commands
